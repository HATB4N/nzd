# 251211

heap에서 malloc & free 반복할 경우 연속 영역에 제대로 할당 못받고 파편화될 가능성 있음.

#include <memory_resource> & std::pmr::...에서 init시점에서 연속적이고 거대한 memory pool을 heap에서 할당받고, 파라미터 관련은 저기서 발급받는 구조로 향후 적용 고려.

직접 alloc하지 말고, 나중에 allocator용 helper를 따로 만들거나.

진행 시점에 따라 파라미터 우선순위 달라짐. 연속 접근을 위해 matrix_T에서 caching한 tranposed / not_tranposed는 동등. 하나만 있으면 나머지 계산 가능. => state나 LRU등에 기반하여 cache eviction 가능할듯? dirty를 최우선으로 날리고.